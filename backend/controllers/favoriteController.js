const Favorite = require('../models/favoriteModel');
const HttpError = require('../models/errorModel');

// ADD FAVORITE MOVIE
// POST api/add-to-favorite/:id
// PROTECTED
const addToFavorite = async (req, res, next) => {
    try {
        const movieId = req.params.id;

    } catch (error) {
        
    }
}